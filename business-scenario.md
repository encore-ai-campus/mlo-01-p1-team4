# 데이터 크롤링 솔루션 비즈니스 시나리오

## 1. 문서 목적

본 문서는 자동차 통계 및 기업 FAQ 데이터를 대상으로 데이터 크롤링 솔루션을 제공할 때, 고객이 겪고 있는 업무상 문제를 어떤 기능으로 해결할 수 있는지 설명한다.

이 솔루션은 단순히 웹 페이지의 내용을 수집하는 도구가 아니다. 허용된 source에서 데이터를 수집하고, 표준 형식으로 정제하며, 출처·수집 시점·처리 결과를 함께 관리해 고객이 데이터를 다시 확인하고 업무에 활용할 수 있도록 지원하는 것을 목표로 한다.

## 2. 고객의 현재 업무상 이슈

### 2.1 이슈 한눈에 보기

| ID | 현재 상황 | 업무상 영향 | 필요한 해결 방향 |
|---|---|---|---|
| `ISSUE-01` | 자동차 통계와 FAQ가 여러 파일·형식으로 분산되어 있음 | 자료 검색과 비교에 시간이 걸리고 담당자별 결과가 달라질 수 있음 | 데이터를 표준 형식으로 통합 수집·관리 |
| `ISSUE-02` | 결과 데이터와 원천 source 정보가 분리되어 있음 | 결과의 정확성 확인과 재검증이 어려움 | 결과마다 source와 수집 시점 연결 |
| `ISSUE-03` | 수집·정제·저장 과정이 하나의 결과로만 관리됨 | 오류 발생 지점과 원인 파악에 시간이 많이 소요됨 | 처리 단계를 구분하고 단계별 상태 기록 |
| `ISSUE-04` | 실패한 작업을 다시 실행할 때 중복 방지 기준이 없음 | 중복 적재 또는 불필요한 전체 재처리 발생 | 실행 식별자와 중복 방지 규칙 적용 |
| `ISSUE-05` | 수집 규칙과 처리 이력이 담당자 경험에 의존함 | 담당자 변경 시 업무 재현과 인수인계가 어려움 | source registry와 인계용 evidence 제공 |
| `ISSUE-06` | API 승인·접근권한 등으로 live source 사용이 지연될 수 있음 | 개발과 검증이 source 승인 일정에 종속됨 | fixture 또는 승인된 XLSX로 핵심 흐름 선검증 |

### 2.2 현재 업무 흐름에서 발생하는 문제

```text
여러 파일에서 자료 확인
        ↓
수작업으로 기준월·category·형식 통일
        ↓
결과와 원천 source를 별도로 확인
        ↓
오류 발생 시 전체 처리 과정을 다시 조사
        ↓
재실행 과정에서 중복 여부를 수동 확인
        ↓
담당자 변경 시 처리 방법을 다시 파악
```

이 흐름의 공통 문제는 데이터가 수집된 이후의 관리 정보가 부족하다는 점이다. 결과값만으로는 데이터의 출처, 처리 과정, 품질 상태, 재실행 이력을 충분히 설명하기 어렵다.

### 2.3 고객이 실제로 필요로 하는 업무 결과

고객이 원하는 것은 단순한 크롤링 결과 파일이 아니라, 다음 질문에 답할 수 있는 데이터이다.

| 고객의 질문 | 필요한 정보 |
|---|---|
| 이 데이터는 어디에서 왔는가? | `source_id`, source URL 또는 fixture 식별자 |
| 언제 수집된 데이터인가? | `collected_at`, 기준월 |
| 정상적으로 처리되었는가? | 단계별 처리 상태와 품질검증 결과 |
| 오류가 났다면 어디서 발생했는가? | `fetch`, `parse`, `validate`, `persist` 중 실패 단계 |
| 다시 실행해도 안전한가? | `run_id`, 입력 식별자, 중복 방지 결과 |
| 다음 담당자가 업무를 이어받을 수 있는가? | source registry, 처리 규칙, sanitized evidence |

## 3. 데이터 크롤링 솔루션의 제공 가치

솔루션은 다음의 흐름으로 고객의 업무를 지원한다.

```text
허용된 source 확인
        ↓
데이터 수집
        ↓
표준 형식으로 정제
        ↓
필수값·중복·품질 검증
        ↓
데이터 및 실행 이력 저장
        ↓
출처가 포함된 결과 제공
```

고객은 결과 데이터뿐 아니라 다음 정보를 함께 확인할 수 있다.

- 어떤 source에서 수집했는가
- 언제 수집했는가
- 어떤 실행에서 생성되었는가
- 어느 처리 단계까지 성공했는가
- 오류가 발생했다면 어느 단계에서 발생했는가
- 재실행 결과가 기존 결과와 어떻게 연결되는가

## 4. 업무 이슈와 솔루션 기능의 대응

| 고객 업무 이슈 | 제공 기능 | 해결 결과 |
|---|---|---|
| 여러 파일에 데이터가 분산됨 | 자동차 통계와 FAQ를 정해진 표준 구조로 수집·저장 | 동일한 기준으로 데이터를 조회하고 비교 가능 |
| 결과의 출처를 찾기 어려움 | `source_id`, source URL 또는 fixture 식별자 저장 | 결과의 원천을 추적하고 재검증 가능 |
| 수집 시점을 알기 어려움 | `collected_at` 및 실행 이력 저장 | 기준 시점별 데이터 확인 가능 |
| 정제·적재 오류 원인을 알기 어려움 | `fetch`, `parse`, `validate`, `persist` 단계별 상태 기록 | 실패 단계와 원인 파악 가능 |
| 재실행 시 중복이 발생함 | `run_id`와 입력 식별자를 이용한 중복 방지 | 동일 입력의 안전한 재실행 가능 |
| 담당자 변경 시 인수인계가 어려움 | source registry, 처리 결과, sanitized evidence 제공 | 다음 담당자가 작업을 재현하고 이어서 수행 가능 |
| live source 승인 전에는 개발이 멈춤 | official-shape fixture 지원 | 실제 credential 없이 핵심 기능 검증 가능 |
| 허용되지 않은 페이지까지 수집할 위험 | allowlist 기반 수집 대상 제한 | 수집 범위와 법적·운영적 경계 통제 |
| 개인정보나 credential이 로그에 남을 위험 | 민감정보 제외 및 sanitized evidence 생성 | 안전한 검토·공유·인계 가능 |

## 5. 주요 제공 기능

### 5.1 허용 source 관리

솔루션 제공자는 고객과 함께 수집 가능한 source를 정의하고, source별 소유자·수집 범위·대체 source·접근 승인 상태를 관리한다.

source registry에는 최소한 다음 정보가 포함된다.

- `source_id`
- source 유형: API, XLSX, HTML page, fixture
- 수집 대상과 범위
- 담당자 또는 승인 책임자
- 접근 승인 상태
- schema 또는 형식 버전
- 수집 불가 시 fallback

이를 통해 담당자가 바뀌어도 “어디에서 무엇을 수집해야 하는지”를 다시 확인할 수 있다.

### 5.2 자동차 통계 데이터 수집

승인된 자동차 통계 source 또는 대응 fixture에서 기준월 1개의 데이터를 수집한다. 수집된 데이터는 기준월·지역·차종·결과값을 기준으로 표준화한다.

고객은 결과 조회 시 통계값만 보는 것이 아니라 기준월과 source 정보를 함께 확인할 수 있다.

### 5.3 FAQ 데이터 수집

allowlist에 등록된 FAQ page 최대 2개를 대상으로 회사·category·질문·답변을 수집한다. 로그인, CAPTCHA, robots, 403, 429를 우회하지 않으며 접근이 제한된 source는 수집 중단 및 검토 대상으로 표시한다.

이를 통해 고객은 여러 FAQ 페이지를 직접 방문하지 않고도 회사·category별 질문과 답변을 확인할 수 있다.

### 5.4 데이터 정제 및 품질 검증

수집된 원천 데이터는 고객이 정한 최소 필드와 형식에 맞게 정제한다.

주요 검증 항목은 다음과 같다.

- 필수 필드 존재 여부
- 빈 질문·빈 답변 여부
- 기준월·지역·차종 값의 형식
- 중복 데이터 여부
- source 식별자 존재 여부
- 수집 시각과 실행 식별자 존재 여부

검증 결과는 데이터와 함께 저장해 결과의 신뢰성을 확인할 수 있도록 한다.

### 5.5 실행 이력 및 실패 단계 관리

각 실행에 `run_id`를 부여하고 다음 단계를 구분해 기록한다.

1. `fetch`: source 접근 및 데이터 수집
2. `parse`: 원천 형식 해석
3. `validate`: 필수값·중복·품질 검증
4. `persist`: 데이터 및 메타데이터 저장

실패 시 실패 단계, 오류 유형, 발생 시각, 재실행 여부를 기록한다. 운영자는 이를 기반으로 전체 재실행이 필요한지, 특정 단계만 다시 확인하면 되는지 판단할 수 있다.

### 5.6 안전한 재실행

동일한 source와 기준월을 다시 처리하더라도 중복 데이터가 생성되지 않도록 입력 식별자와 실행 식별자를 관리한다.

최소한 다음 조건을 보장한다.

- 동일 입력을 여러 번 실행해도 중복 레코드가 생성되지 않는다.
- 실패 실행과 성공한 재실행을 `run_id`로 추적할 수 있다.
- 재실행 결과가 기존 결과를 무단으로 덮어쓰지 않는다.
- 실행 결과와 evidence가 서로 연결된다.

### 5.7 검토 및 인계용 evidence 제공

검토자에게는 실제 credential, 개인정보, private endpoint가 포함되지 않은 sanitized evidence를 제공한다.

evidence에는 다음 정보를 포함한다.

- 처리한 source 식별자
- 실행 일시
- 처리 대상 범위
- 단계별 성공·실패 상태
- 품질 검증 결과
- 재실행 결과
- 미해결 위험과 제한사항

## 6. 업무별 해결 시나리오

### 6.1 역할별 업무 변화 한눈에 보기

| 업무 역할 | 기존 업무의 어려움 | 솔루션 적용 후 업무 | 업무상 효과 |
|---|---|---|---|
| 분석 담당자 | 여러 파일에서 기준월·지역·차종을 직접 맞추고 출처를 별도로 확인 | 표준화된 결과에서 조건별 데이터를 조회하고 `source_id`, 수집 시점을 함께 확인 | 분석 준비 시간 단축, 결과 신뢰성 향상 |
| 파이프라인 운영자 | 오류 발생 시 전체 처리 과정을 다시 확인하고 원인을 추정 | 실행 이력에서 `fetch`·`parse`·`validate`·`persist` 중 실패 단계를 확인 | 장애 원인 파악 및 재실행 판단 용이 |
| 검토자 | 결과 파일과 원천 자료를 수작업으로 대조 | source, 수집 시점, 품질 결과가 포함된 evidence를 확인 | 검토 시간 단축, 감사 가능성 확보 |
| 신규 담당자 | 이전 담당자에게 수집 대상과 실행 방법을 다시 문의 | source registry와 handoff snapshot으로 범위·fallback·최근 실행 결과 확인 | 업무 인수인계와 재현성 향상 |

### 6.2 역할별 상세 시나리오

#### 분석 담당자

```
기준월·지역·차종 선택
        ↓
표준화된 자동차 통계 조회
        ↓
결과값과 source·수집 시점 확인
        ↓
분석 또는 결과 비교
```

**해결되는 문제**

- 여러 파일을 직접 조합하지 않아도 된다.
- 동일 기준월의 결과를 같은 형식으로 비교할 수 있다.
- 결과값의 출처를 별도 문서에서 다시 찾지 않아도 된다.

#### 파이프라인 운영자

```
실행 상태 확인
        ↓
실패 단계 식별
        ↓
오류 및 evidence 확인
        ↓
재실행 여부 판단
        ↓
중복 없이 재실행
```

**해결되는 문제**

- 오류 원인을 수집·파싱·검증·저장 단계로 나누어 확인할 수 있다.
- 동일 입력을 다시 실행해도 중복 여부를 검증할 수 있다.
- 실패 실행과 재실행 결과를 `run_id`로 연결할 수 있다.

#### 검토자

```
결과 데이터 확인
        ↓
source·수집 시점 확인
        ↓
품질검증 결과 확인
        ↓
처리 상태 및 evidence 검토
        ↓
검토 결과 기록
```

**해결되는 문제**

- 결과와 원천 source를 수작업으로 대조하는 부담이 줄어든다.
- 필수값 누락, 중복, 처리 실패 여부를 확인할 수 있다.
- credential이나 개인정보가 제거된 evidence를 안전하게 공유할 수 있다.

#### 신규 담당자

```
source registry 확인
        ↓
수집 범위·처리 규칙 확인
        ↓
최근 실행 결과 확인
        ↓
fixture 또는 fallback 확인
        ↓
동일 업무 재현
```

**해결되는 문제**

- 담당자의 개인 경험에 의존하지 않고 업무를 파악할 수 있다.
- 승인된 source와 수집하면 안 되는 대상을 구분할 수 있다.
- 인계 snapshot을 기준으로 동일한 처리 결과를 재현할 수 있다.

### 6.3 솔루션 적용 전후 비교

| 구분 | 적용 전 | 적용 후 |
|---|---|---|
| 데이터 확인 | 여러 파일과 페이지를 수동 확인 | 표준화된 결과로 통합 확인 |
| 출처 확인 | 별도 문서 또는 담당자에게 문의 | 결과 데이터의 `source_id`로 확인 |
| 오류 대응 | 전체 과정을 다시 조사 | 실패 단계와 오류 정보 확인 |
| 재실행 | 중복 여부를 수동 확인 | `run_id`와 입력 식별자로 검증 |
| 인수인계 | 담당자 경험과 구두 설명 의존 | registry·evidence·snapshot 활용 |
| source 미승인 상태 | 개발 또는 검증 중단 | fixture로 핵심 업무 흐름 선검증 |

## 7. PoC에서 고객이 받는 결과물

- 자동차 통계 기준월 1개 처리 결과
- FAQ page 최대 2개 처리 결과
- 표준화된 결과 데이터
- source 및 실행 메타데이터
- 단계별 성공·실패 기록
- 안전한 재실행 검증 결과
- sanitized evidence
- `output/handoff_snapshot.json` 인계 파일
- live source 미승인 시 fixture 사용 내역
- 제한사항 및 향후 운영 전환 시 고려사항

## 8. 3일 PoC의 성공 기준

PoC는 다음을 입증하면 성공으로 본다.

- 자동차 통계와 FAQ 데이터를 각각 최소 1회 처리할 수 있다.
- 결과 데이터와 source 정보를 함께 확인할 수 있다.
- 필수값 누락과 중복을 검출할 수 있다.
- 오류 발생 단계와 재실행 결과를 추적할 수 있다.
- 동일 입력을 재실행해도 중복이 발생하지 않는다.
- 실제 credential 없이도 fixture로 핵심 흐름을 재현할 수 있다.
- 다음 담당자가 인계 파일만으로 실행 결과와 제한사항을 이해할 수 있다.

## 9. 제공 범위의 한계

본 솔루션은 3일 PoC 범위에서 데이터 크롤링 업무의 핵심 흐름을 검증한다. 따라서 다음은 별도 프로젝트에서 확장해야 한다.

- 다수 source와 대규모 데이터 처리
- production 배포 및 CI/CD
- 고가용성, 재해복구, 자동 failover
- 실시간 모니터링과 알림
- 복잡한 인증 및 권한 연계
- 데이터 품질 규칙의 고도화
